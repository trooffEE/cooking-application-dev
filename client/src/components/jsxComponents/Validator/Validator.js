// // import { setIsLoginValid, setIsPasswordValid } from "../../../redux/actionCreators/loginActionCreator";

// const validator = (type, value) => {
//     let fieldErr = {validated: true};

//     if (type === 'text') {
//         if (value.length < 3) fieldErr = {...fieldErr, loginErr: true, validated: false};
//     } 

//     else if (type === "password") {
//         if (value.length < 7) fieldErr = {...fieldErr, passwordErr: true, validated: false};
//     }

//     return {};
// }

// export default validator;
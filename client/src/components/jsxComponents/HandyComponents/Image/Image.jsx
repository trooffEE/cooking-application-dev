import React from 'react';
import { StyledImage } from '../../../styledComponents/HandyComponents/HandyComponents';

const Image = (props) => {

    return (
        <StyledImage {...props}/>
    );
}

export default Image;
